build:
  # Specify the build environment with Node 20
  docker:
    web:
      # Use Node 20 image
      image: node:20

      # Define build steps
      command:
        - npm install
        - npm run build

run:
  web: npm start