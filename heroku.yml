setup:
  addons:
    - plan: heroku-postgresql  # Optional, add if your project requires a database

build:
  docker:
    web: Dockerfile  # The Dockerfile will define how to build your app

run:
  web: npm start  # Command to start the app